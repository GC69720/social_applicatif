---
name: secrets

'on':
  push:
    branches: ['main']
  pull_request:
    branches: ['main']
  workflow_dispatch: {}

permissions:
  contents: read
  security-events: write   # nécessaire pour l’upload SARIF par le workflow appelé

jobs:
  gitleaks:
    uses: GC69720/social_ci-cd/.github/workflows/secret-scan.yml@v1
